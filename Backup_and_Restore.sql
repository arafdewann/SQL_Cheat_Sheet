
-- Backup a database (this command is typically executed in the command line interface, not inside an SQL session)
-- mysqldump -u [user] -p[password] [database_name] > backup.sql

-- Restore a database (this command is typically executed in the command line interface, not inside an SQL session)
-- mysql -u [user] -p[password] [database_name] < backup.sql
